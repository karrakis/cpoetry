<%= form_for(bill_word) do |f| %>
  <% if bill_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bill_word.errors.count, "error") %> prohibited this bill_word from being saved:</h2>

      <ul>
      <% bill_word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bill_kid %>
    <%= f.number_field :bill_kid %>
  </div>

  <div class="field">
    <%= f.label :word_kid %>
    <%= f.number_field :word_kid %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
